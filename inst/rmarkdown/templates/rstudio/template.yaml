name: Default Word Document Template
description: >
  This is the template generated by default by RStudio. This is probably only useful as a starting point for making other templates.
create_dir: false
